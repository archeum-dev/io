{
  "name": "archeum-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "archeum.io - static marketing site (no framework, scroll-alive).",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prune-dist.mjs",
    "preview": "vite preview",
    "assets": "node scripts/gen-assets.mjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.0",
    "playwright": "^1.61.0",
    "sharp": "^0.35.1",
    "vite": "^7.2.4"
  }
}
